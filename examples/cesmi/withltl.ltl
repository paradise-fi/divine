/* Atomic proposition a is defined as (a==2). See prop_a function in withltl.c
 * file.

 * Also any state where (b==2) is defined as a goal state.  See get_flags
 * function in withltl.c file. Check for the reachability for a goal state can
 * be performed with verification for safety (property p_1), see output of 
   $ divine info withltl.so 
 */

#property !(F(a))
#property F(a)
#property F(!a)
